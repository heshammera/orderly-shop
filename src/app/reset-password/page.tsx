"use client";
import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Page(){const [password,setPassword]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <main dir="rtl" className="max-w-md mx-auto p-8 space-y-6"><h1 className="text-2xl font-bold">تعيين كلمة مرور جديدة</h1><form onSubmit={async e=>{e.preventDefault();setBusy(true);try{const db=createClient();const {data}=await db.auth.getSession();if(!data.session){setMessage('الرابط غير صالح أو انتهت صلاحيته؛ اطلب رابطًا جديدًا.');return;}const {error}=await db.auth.updateUser({password});setMessage(error?'تعذر تغيير كلمة المرور':'تم تغيير كلمة المرور بنجاح');if(!error){await db.auth.signOut();}}finally{setBusy(false);}}}><label>كلمة المرور الجديدة<input className="border rounded p-3 w-full my-4" type="password" minLength={12} required autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy} className="bg-primary text-white p-3 rounded">حفظ كلمة المرور</button></form><p role="status">{message}</p><a href="/login">تسجيل الدخول</a></main>}

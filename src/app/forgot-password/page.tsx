"use client";
import {useState} from 'react';
export default function Page(){const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);
return <main dir="rtl" className="max-w-md mx-auto p-8 space-y-6"><h1 className="text-2xl font-bold">استعادة كلمة المرور</h1><form className="space-y-4" onSubmit={async e=>{e.preventDefault();setBusy(true);try{const r=await fetch('/api/auth/request-reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message||d.error);}catch{setMessage('تعذر الاتصال بالخدمة');}finally{setBusy(false);}}}><label className="block">البريد الإلكتروني<input className="border rounded p-3 w-full mt-2" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button disabled={busy} className="bg-primary text-white rounded p-3 w-full">{busy?'جارٍ الإرسال…':'إرسال رابط الاستعادة'}</button></form><p role="status">{message}</p><a href="/login">العودة لتسجيل الدخول</a></main>}
